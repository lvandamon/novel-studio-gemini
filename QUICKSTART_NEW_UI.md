# Novel Studio Gemini - 新UI快速启动指南

## 🚀 快速开始

### 1. 环境准备

确保已安装:
- Python 3.12+
- Node.js 18+
- uv (Astral包管理器)

### 2. 初始化数据库 (首次运行必须)

```bash
# 初始化世界观数据
uv run python utils/init_world_v2.py

# 初始化风格库
uv run python utils/init_style.py
```

### 3. 启动后端API

```bash
cd backend
uv run uvicorn api.main:app --reload --port 8000
```

后端会在 `http://localhost:8000` 启动,你会看到:
```
✅ System Ready
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### 4. 启动前端开发服务器

```bash
cd frontend
npm install  # 首次需要安装依赖
npm run dev
```

前端会在 `http://localhost:5173` 启动。

### 5. 访问界面

在浏览器打开: **http://localhost:5173**

## 🎨 新UI功能导览

### 首次使用流程

1. **选择章节**: 左侧侧边栏默认显示章节1
2. **生成第一章**:
   - 中央区域会显示"点击'生成本章'开始创作"
   - 点击右上角蓝色 **"生成本章"** 按钮
3. **观察AI工作**:
   - 顶部会显示当前执行的Agent (如 "Director 规划中...")
   - 底部进度条显示6个Agent的状态
4. **等待AI完成**:
   - Director → Editor → Writer → Simulator → Reviewer → Archivist
   - 如果某个环节需要人工干预,会在对应Agent暂停
5. **查看和编辑内容**:
   - 章节内容会出现在中央编辑器
   - 你可以直接编辑文本
   - 如果大纲需要修改,会在黄色背景区域显示

### 核心功能

#### 📖 章节管理 (左侧边栏)
- **新建章节**: 点击顶部 `+` 按钮
- **切换章节**: 点击章节卡片
- **搜索章节**: 使用顶部搜索框
- **查看统计**: 底部显示已完成/草稿/总计

状态图标说明:
- ✅ 绿色: 已完成
- ⏰ 黄色: 草稿中
- ⚠️ 红色: 有错误
- 📄 灰色: 未开始

#### ✍️ 写作工作室 (中央区域)
- **章节导航**: 使用 `< >` 按钮切换上下章
- **生成章节**: 点击 **"生成本章"** (蓝色按钮)
- **继续创作**: 如果AI暂停,点击 **"继续创作"** (紫色按钮)
- **编辑内容**: 直接在编辑器中修改文本
- **查看大纲**: 黄色区域显示章节大纲

#### 🤖 AI助手面板 (右侧)

切换Tab查看不同信息:

**Agents** - 查看AI工作状态
- 蓝色脉动 = 正在执行
- 绿色勾选 = 已完成
- 灰色圆点 = 等待中

**角色** - 查看角色库
- 显示所有活跃角色
- 包含境界、动机、物品等信息

**指标** - 查看质量评估
- 剧情逻辑分数
- 风格一致性分数
- 物理规则检查
- 张力值

#### 🌍 世界观管理 (顶部导航)
点击 **"世界观"** 按钮打开大窗口:
- **知识图谱**: 可视化角色/事件/物品关系网络
- **角色库**: 详细的角色档案管理
- **物品系统**: (开发中)
- **世界设定**: (开发中)

#### 📥 导出功能 (顶部导航)
点击 **"导出"** 按钮:
- 选择起始章节和结束章节
- 选择格式: TXT (适合起点/晋江) 或 EPUB (电子书)
- 点击生成并下载

## 🆘 常见问题处理

### 逻辑死锁警告

如果屏幕底部出现红色警告条:
```
⚠️ Logic Deadlock - Human Intervention Required
```

说明Simulator检测到逻辑冲突(例如角色用了不存在的物品)。

**处理方式**:
1. **阅读原因**: 看具体是什么逻辑问题
2. **选择操作**:
   - `FORCE PASS`: 强制通过,忽略逻辑检查(不推荐)
   - `FIX IN WORLD PANEL`: 前往世界观面板修正角色/物品数据
   - `DISMISS`: 暂时忽略警告

### AI卡在某个Agent不动

如果某个Agent长时间显示蓝色脉动:
1. 检查后端终端是否有错误日志
2. 检查网络连接 (DeepSeek API)
3. 尝试刷新页面重新加载
4. 查看后端日志文件

### 章节内容不显示

可能原因:
1. 章节还未生成 (状态为"空白")
2. 工作流还在执行中 (等待AI完成)
3. API连接失败 (检查后端是否启动)

解决方法:
- 打开浏览器开发者工具 (F12) 查看Console错误
- 检查Network标签,确认API请求是否成功

## 🎯 使用技巧

### 1. 自定义章节指令
在生成章节前,可以在"指挥中心"输入导演指令:
```
例如:这一章要重点刻画主角的挣扎,增加内心戏
```

### 2. 修改大纲
如果AI生成的大纲不满意:
- 直接在黄色大纲区域编辑
- 修改后点击 **"确认并撰写草稿"**
- AI会根据新大纲重新生成内容

### 3. 查看角色关系
在世界观管理面板:
- 点击"知识图谱"标签
- 使用时间滑块筛选特定章节范围
- 点击节点查看详细关系

### 4. 批量导出
导出功能支持批量:
```
起始章节: 1
结束章节: 50
格式: TXT
```
会生成一个包含50章的完整文件。

## 🔧 高级配置

### 修改轮询间隔
在 `frontend/src/components/WritingStudio.tsx`:
```typescript
const interval = setInterval(fetchState, 3000); // 改为5000 = 5秒轮询
```

### 调整章节加载数量
在 `frontend/src/components/ChapterSidebar.tsx`:
```typescript
for (let i = 1; i <= 20; i++) { // 改为50 = 加载前50章
```

### 自定义Agent颜色
在 `frontend/src/components/AIAssistantPanel.tsx` 修改 `agentStages` 数组。

## 📊 性能优化建议

1. **关闭不需要的面板**: 使用折叠按钮隐藏章节列表或AI助手面板
2. **减少轮询频率**: 如果章节很多,可以增加轮询间隔
3. **限制章节加载数**: 不需要一次性加载所有章节
4. **使用章节搜索**: 直接搜索跳转,避免滚动长列表

## 🎨 界面定制

### 切换夜间模式
点击顶部导航栏的月亮图标 🌙 (功能已实现,样式待完善)

### 调整编辑器字体
在 `WritingStudio.tsx` 的 textarea 样式中修改:
```typescript
className="... text-lg font-serif ..." // 改为 text-xl 或 text-base
```

## 🐛 调试模式

如果需要查看详细的状态信息:
1. 打开浏览器开发者工具 (F12)
2. 切换到 Console 标签
3. 会看到API请求和状态更新日志
4. 切换到 Network 标签查看API响应数据

## 📚 相关文档

- [UI设计文档](./frontend/UI_REDESIGN.md)
- [项目说明](./CLAUDE.md)
- [API文档](./backend/api/main.py)

---

**需要帮助?** 查看后端日志或前端Console获取详细错误信息。
